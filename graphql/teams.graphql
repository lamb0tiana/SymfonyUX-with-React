query teamList($count: Int!, $cursor: String = "MA==" ){
    team_collectionTeams(first: $count, after: $cursor) {
        totalCount
        edges {
            cursor
            node {
                id
                slug
                name
                isocode: countryCode
                funds: moneyBalance
            }
        }
    }
}
