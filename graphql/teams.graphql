query teamList($count: Int!, $cursor: String = "LTE=" ){
    team_collectionTeams(first: $count, after: $cursor) {
        totalCount
        edges {
            cursor
            node {
                id: _id
                slug
                name
                isocode: countryCode
                funds: moneyBalance
            }
        }
    }
}
