mutation createPlayer($input: createPlayerInput!){
    createPlayer(input: $input){
        player {
            name
            slug
            id
        }
    }
}

mutation updateWorth($input: updateWorthPlayerInput!){
    updateWorthPlayer(input: $input) {
        player {
            id
            worth
            name
            surname
        }
    }
}