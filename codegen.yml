schema:
  - "http://localhost:8888/schema.graphql"
documents:
  - "./graphql/**/*.{gql,graphql}"
generates:
  ./assets/queries/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo